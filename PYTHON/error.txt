SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules WHERE title LIKE "%w%";
SELECT idRules, totalVector, title, description FROM Rules WHERE title LIKE "%wh%";
SELECT idRules, totalVector, title, description FROM Rules WHERE title LIKE "%wher%";
SELECT idRules, totalVector, title, description FROM Rules WHERE title LIKE "%wher%";
SELECT idRules, totalVector, title, description FROM Rules WHERE title LIKE "%whe%";
SELECT idRules, totalVector, title, description FROM Rules WHERE title LIKE "%wh%";
SELECT idRules, totalVector, title, description FROM Rules WHERE title LIKE "%w%";
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
SELECT idRules, totalVector, title, description FROM Rules;
